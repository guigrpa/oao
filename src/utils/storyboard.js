// Replace by 'storyboard-preset-console' and update root package.json when
// the first v3 beta is published
import storyboard from 'storyboard/lib/withConsoleListener';

export default storyboard;
export * from 'storyboard/lib/withConsoleListener';
